FROM kalilinux/kali-rolling
RUN apt update && apt install -y \
 assetfinder amass subfinder findomain \
 python3 python3-pip dnsutils
COPY . /app
WORKDIR /app
RUN pip3 install flask cryptography mysql-connector-python
ENTRYPOINT ["bash","recon.sh"]
